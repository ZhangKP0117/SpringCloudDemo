eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8762
spring:
  application:
    name: service-hi

#  需要指明spring.application.name,这这在以后的服务与服务之间相互调用一般都是根据这个name 。
#  启动工程，打开http://localhost:8761 ，即eureka server 的网址：